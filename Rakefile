# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'git_r_backup' do
  developer('Slippy Douglas', 'git_r_backup@slippyd.com')

  # self.rubyforge_name = 'git_r_backupx' # if different than 'git_r_backup'
end
